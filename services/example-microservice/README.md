# example-microservice

A tiny stdlib-only inventory service (two endpoints over an in-memory store) used
as the real codebase-lane target for the `software-arch` demo. Run with
`python app.py`; `GET /items`, `POST /items`.
